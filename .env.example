# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# ComfyUI Configuration
COMFYUI_URL=http://localhost:8188

# ComfyUI Workflow Configuration
# 方式1（推荐）: 使用 workflow.json 文件（默认）
#   - 直接编辑项目目录下的 workflow.json 文件
#   - 不需要设置 WORKFLOW_JSON 环境变量
#
# 方式2（可选）: 通过环境变量设置工作流（适合动态部署）
#   - 将完整的工作流 JSON 字符串设置到 WORKFLOW_JSON 环境变量
#   - 示例: WORKFLOW_JSON={"88":{"inputs":{"width":"%width%",...}},...}
#   - 如果设置了此变量，将优先使用它而不是 workflow.json 文件
# WORKFLOW_JSON=

# Optional: Data directory for deployment
# DATA_DIR=/data

# Optional: Set for deployment
# ZEABUR=true
